# Cursor — local session state (do not commit)
# **/ also covers stray nested state dirs (e.g. scripts/.cursor/state/ from a
# script run with the wrong -WorkspaceRoot)
**/.cursor/state/
# rule-mined-report.* lives under .cursor/state/ (covered above)
.cursor/quality-gate.json
.cursor/guard-shell.local.json

# Generated / runtime agent artifacts
docs/agent/rule-candidates.ndjson

# Local workflow / diagram assets (regenerate locally; not kit SSOT)
assets/

# LLM wiki raw dumps (may contain sensitive originals; only distilled notes are committed)
docs/wiki/_raw/

# Machine-local Obsidian ingest config (copy from _config/obsidian-repos.example.json)
_config/obsidian-repos.json
.obsidian-ingest.json

# Local harness / stdin test sandboxes (not kit SSOT)
_guard-test/
_readstdin.ps1

# Secrets and environment
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db
Desktop.ini

# Editor
.idea/
.vscode/
*.swp
*~

# Dependencies / build
node_modules/
dist/
build/

# Python cache
__pycache__/
*.py[cod]

# Git worktrees (local only)
.worktrees/

# Logs
*.log

.claude/settings.json